# public/

Static assets served at the root URL path (`/filename`). Reference these files directly in `src` or `href` attributes — no import needed.

## Files

### Images
| File | Used in |
|------|---------|
| `sri.jpeg` | `Navbar` — profile photo in sidebar and mobile drawer |
| `hero.png` | `MainContent` — full-bleed background of the HOME section |
| `cwru-logo.png` | `MainContent` — Case Western Reserve University logo in ABOUT section |
| `deloitte-logo.svg` | `MainContent` — Deloitte logo in ABOUT section; also used as a sample carousel image in `scratchpad/` |
| `favicon.ico` | Browser tab icon (also referenced in `app/` as `app/favicon.ico`) |

### Social / UI Icons
| File | Used in |
|------|---------|
| `github-logo.svg` | `Navbar` — GitHub social link icon |
| `linkedin-logo.svg` | `Navbar` — LinkedIn social link icon |
| `twitter-logo.svg` | `Navbar` — Twitter/X social link icon |
| `download.svg` | `MainContent` — icon on the resume download button in the HOME section |

### Next.js Defaults
| File | Notes |
|------|-------|
| `next.svg` | Default Next.js logo (not currently used in the portfolio) |
| `vercel.svg` | Default Vercel logo (not currently used in the portfolio) |
| `file.svg`, `window.svg`, `globe.svg` | Default scaffold icons (not currently used in the portfolio) |
