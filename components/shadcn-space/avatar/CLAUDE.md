# components/shadcn-space/avatar/

Usage examples for the `Avatar` component from `components/ui/avatar.tsx`.

## Files

### `avatar-01.tsx`
Demonstrates the default `Avatar` with a remote image source and a text fallback (`"CN"`). Shows the `AvatarImage` + `AvatarFallback` composition pattern.
